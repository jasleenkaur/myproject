{% extends 'blog/base.html' %}

{% block content %}
    <div class="post">
        <h2>{{ post.title }}</h2>
    <p>{{ post.pub_date }}</p>
        <p>{{ post.post_content|linebreaks }}</p>
                
        
    </div>
{% endblock %}
